### dry-validation-issue

For custom predicates, using `locale` as the block parameter name will produce `Dry::Validation::MissingMessageError`.

Look at `person.rb` for a simple example of the issue.
